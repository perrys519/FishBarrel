# Build artefacts
*.zip
*.crx
*.pem

# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
*.bak
.vscode/
.idea/

# Node (in case anything is added later)
node_modules/

# Per-authority audit screenshots (referenced from AUDIT-REPORT.md but
# kept out of the repo because they're large binary diffs).
audit-screenshots/
